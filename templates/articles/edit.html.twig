{% extends 'base.html.twig' %}

{% block title %}Qiwogames -Editer Articles-{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row table-container">
            <br />
            <div class="col-sm-3 left-container">
                {% include 'includes/leftmenu.html.twig' %}
            </div>
            <br />
            <div class="col-sm-9 right-container">
                <h1 class="h1-titre list-group-item list-group-item-primary active"><i class="fas fa-gamepad"></i> EDITER ARTICLES :</h1>
                <div class="animated slideInRight">
                    {{ include('articles/_form.html.twig', {'button_label': 'Update'}) }}
                    <div class="right-content">
                        <a class="btn btn-outline-warning" href="{{ path('articles_index') }}">Annuler</a>

                        {{ include('articles/_delete_form.html.twig') }}
                    </div>
                </div>

            </div>

        </div>
    </div>
{% endblock %}
